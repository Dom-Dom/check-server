# Usage
This project allows the configuration of the server port and the commands that are run via a `config.json` file that is required.
A `config.json.dist` file is availble in the `config` folder as a template.

Requires nodeJS and npm

The server can be run with:
```
npm install
node index.js
```
